##Getting Started and initialisation of prototype build##
-------------------

To initialize your local repository using the RAINDROP OS tree, use a command like this:

    repo init -u git://github.com/RaindropOS/platform_manifest.git

Then to sync up:

    repo sync
  
  
